def count_substring(string, sub_string):
    len_string = len(string)
    len_sub = len(sub_string)
    num_occ = 0
    
    for i in range(0, len_string - len_sub + 1):
        if string[i:i + len_sub] == sub_string:
            num_occ += 1
    return num_occ
